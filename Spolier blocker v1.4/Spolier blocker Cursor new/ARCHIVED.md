Historical source; not a release target.

Build and load the repository-root `extension/` directory for version 2.0. The root build allowlist excludes this tree and old ZIPs. Targeted security fixes are backported here, but the old UI and detection variants are not supported or certified by the new release tests.
